import { StyleSheet, Text, View } from 'react-native';
import { AppsList } from "./components/pages/app-list"
import { AppNavigator } from "./components/app-navigator"

export default function App() {
  return (
    <AppNavigator />
  )
}

const styles = StyleSheet.create({})
